//Caso não esteja padrão
// Ir no flecha do lado do + no terminal - Select Default Profile - Selecionar Git Bash

// Instalar comandos no terminal
//  npm install typescript -D
// npx tsc --init




// Pasta ts.config.json

{
  "compilerOptions": {
     "rootDir": "./src",
     "outDir": "./dist",
    "module": "commonjs",
    "target": "es6",
    "strict": true,
  }
}
